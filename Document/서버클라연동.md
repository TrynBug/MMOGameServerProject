# 로그인

1. 클라가 로그인요청
로그인 씬에서 로그인서버에 LoginReq (id, password) 전송

2. 로그인 서버가 LoginReq 받음
AccountDB 에서 id, password 조회해서 검증, 인증토큰 발급 (8byte)
게이트웨이 서버에 LoginAuthTokenNtf (userId, token, 만료시간) 전송
클라에게 LoginRes 전송 (성공, gatewayIP, port, userId, authToken)

3. 게이트웨이서버가 LoginAuthTokenNtf 받음
pendingAuth map에 저장

4. 클라가 게이트웨이서버에 접속하여 GatewayAuthReq (userId, authToken) 요청 

5. 게이트웨이서버가 GatewayAuthReq 받음
pendingAuth map에서 인증토큰 검증
게임서버 선택 (유저수 적은 곳 또는 이전 접속 캐시)
게임서버에 GatewayUserEnterNtf (userId, gatewayId, clientIp) 전송

6. 게임서버가 GatewayUserEnterNtf 받음
DB에서 캐릭터 목록 로드
User 객체 생성 + map 등록
SystemStage(캐릭터선택용 스테이지)에 UserEnter push
클라에게 GameEnterNtf, CharacterListNtf(캐릭터 0~N개) 전송함

7. 클라가 GameEnterNtf 받음
지금은 하는일 없음

8. 클라가 CharacterListNtf 받음
현재 로그인 씬이기 때문에 CharacterListNtf 를 어딘가에 저장해두고 캐릭터선택 씬으로 이동함. 캐릭터선택 씬에서 CharacterListNtf 를 계속 처리함
캐릭터가 없으면 게임서버에 CharacterCreateReq 패킷으로 캐릭터생성 요청. 그러면 게임서버가 캐릭터 생성하고 CharacterCreateRes 보내줌
캐릭터가 있으면 CharacterSelectReq 패킷으로 캐릭터선택 요청

9. 게임서버가 CharacterSelectReq 받음
DB에서 캐릭터 로드, Character 객체 생성, User <-> Character 연결
SystemStage에서 UserLeave psuh 하고 적절한 스테이지에 UserEnter push
클라에게 CharacterSelectRes 전송. 캐릭터ID, StageID, Position 등
 
10. 클라가 CharacterSelectRes 받음
캐릭터를 생성하고 게임 씬으로 이동

11. 게임서버에서 Character가 적절한 스테이지에 입장되면 Stage는 클라에게 ObjectVisibilityNtf 로 스테이지의 오브젝트 정보를 전송함
그리고 입장이 완료되면 StageEnterNtf (stageId 등) 전송  

12. 클라가 게임 플레이를 시작

# 캐릭터 스탯의 연동
1. 게임서버가 CharacterSelectReq 받으면 캐릭터를 생성하고, 캐릭터에게 스탯을 적용한다. 그리고 클라에게 CharacterSelectRes 를 보낸다.
2. 게임서버에서 캐릭터가 Stage에 입장하면 이 순서로 클라에게 패킷을 보낸다:
SendStatUpdateNtf(캐릭터의 모든 스탯)  →  SendHpMpNtf(캐릭터의 현재 HP/MP)  →  StageEnterNtf(입장 완료)
3. 클라가 SendStatUpdateNtf 를 받으면 캐릭터의 스탯을 받은것으로 교체하고, SendHpMpNtf 를 받으면 캐릭터의 현재 HP/MP를 받은것으로 교체한다.
4. 이후 서버에서 스탯에 변경사항이 있으면 클라에게 SendStatUpdateNtf 를 보내고, 현재 HP/MP 변경사항이 있으면 SendHpMpNtf 를 보낸다.


# 캐릭터 위치 연동

## 기본 원칙
클라가 먼저 즉시 반응 — 입력 응답성 우선. 마우스 클릭하면 클라는 서버 응답 안 기다리고 바로 움직임.
서버가 진실의 원천 (source of truth) — 같은 NavMesh 로 같은 시뮬레이션 돌림. 클라/서버가 어긋나면 서버가 맞음.
클라가 자기 캐릭터(LocalPlayer)는 자기가 시뮬레이션 — 서버의 MoveNtf 로 자기 캐릭터를 갱신하지 않음. 보정 패킷 받았을 때만 서버 위치로 스냅.
다른 캐릭터는 서버 MoveNtf 따라감 — 클라는 서버가 보낸 목적지로 자기 NavMesh 경로 재계산해서 이동.
좌표계 통일: 서버/클라 모두 Unity 와 동일 (X, Y, Z). Y=높이, X-Z=평면. yaw 는 Y축 회전, degree.

## 이동 시작 흐름 (마우스 클릭)
1. 유저가 마우스 왼쪽 클릭
	- 클라는 즉시 NavMesh 길찾기, waypoint 따라 이동 시작. 
		- 마우스 위치는 계속 변경되기 때문에 200ms 마다 dest를 갱신하고 NavMesh로 길을찾음
	- 클라는 500ms 마다 서버에게 MoveDestReq{ dest, pos } 송신
2. 서버가 MoveDestReq 받음
	- 클라 pos ↔ 서버 pos 의 X-Z 거리 비교
	- tolerance 초과하면 MovePosCorrectNtf{ 서버pos } 송신 (클라 위치는 인정 안 함)
		- 클라에서는 Character::SetDestination(dest) 로 위치 보정 및 경로 다시 계산
	- MoveNtf 를 주변 sector 캐릭터들에게 broadcast
		- 다른 클라들이 MoveNtf 받으면 해당 캐릭터의 dest를 세팅하고 이동 시작
        
3. 서버: 50ms tick 마다 Character::Update
	- waypoint 따라 이동 시뮬레이션. 마지막 waypoint 도달 시 정지.

## 이동 갱신 흐름 (마우스 끌기)
1. 클라가 마우스를 누르고 있는 동안:
	- 200ms 마다: 마우스 위치 갱신 → NavMesh 경로 재계산 → 새 waypoint 따라감
	- 500ms 마다: MoveDestReq{ 새 dest, 현재 pos } 송신

2. 서버
	- MoveDestReq 받을 때마다 (= 500ms 마다):
		- 위치 검증 (필요 시 보정 패킷)
		- Character::SetDestination 호출 → waypoint 재계산
		- MoveNtf 브로드캐스트
	- 50ms tick 마다 Character::Update (시뮬레이션 계속)
	
알려진 어긋남: 방향이 자주 바뀔 때 클라 200ms vs 서버 500ms 차이로 1~2m 누적. tolerance 3.0m 이내라 정상 통과.

## 정지 흐름 (마우스 떼기)
1. 유저가 마우스 뗌
	- 클라: LocalPlayer 즉시 정지, MoveStopReq{ pos, yaw } 송신
        
2. 서버: MoveStopReq 받음
	- 클라 pos ↔ 서버 pos 의 X-Z 거리 비교
		- 오차범위 이내 → Character::StopAt(클라 pos)  (클라 위치 인정)
		- 오차범위 초과 → Character::StopAt(서버 pos) + MovePosCorrectNtf 송신
		- MoveNtf 브로드캐스트 (is_moving=false)
        
3. 다른 클라들이 MoveNtf 받음 → 해당 캐릭터의 위치를 받은 pos 로 갱신, 정지

MoveDestReq 와의 차이: MoveStopReq 는 tolerance 이내일 때 클라 위치를 서버에 반영. 정지는 정확해야 하니까. 이동 중에는 서버가 진실이지만 정지는 클라가 진실에 가까움.

## 위치 보정 흐름 (MovePosCorrectNtf 수신)
1. 클라가 MovePosCorrectNtf{ pos, yaw } 받음
	- LocalPlayer.SetPosition(pos, yaw) 호출, 위치 즉시 스냅 + 진행 중이던 이동 취소
	- 마우스를 여전히 누르고 있다면 다음 프레임에 MouseInputHandler 가 자동으로 SetMoveDestination(현재 마우스 위치) 호출하고 새 위치 기준으로 NavMesh 경로 재계산
	
자연스러운 재개. 클라는 보정 받았다는 사실 외에 따로 신경쓸 거 없음.

## 다른 캐릭터 (LocalPlayer 가 아닌) 처리
1. 클라가 MoveNtf 받음
	- 다른 캐릭터의 MoveNtf 일 경우 다른캐릭터객체에서 SetMoveDestination 호출하여 dest 설정, 경로 계산. 그러면 다른캐릭터가 Update될때 경로에 따라 이동함

## 관련 파일
- .proto
Proto/GamePacket/move_packet.proto — MoveDestReq, MoveStopReq, MoveNtf, MovePosCorrectNtf

- 서버
Stage.cpp OnUserPacket() — MoveDestReq/MoveStopReq 핸들러, 위치 검증
Stage.cpp broadcastMoveNtf() — sector AOI 내 캐릭터들에게 MoveNtf
Stage.cpp updateCharacters() — 50ms tick 마다 Character::Update 호출
Character.cpp SetDestination() — Stage::FindPath 호출, waypoint 채우기
Character.cpp Update() — waypoint 따라 이동
Character.cpp StopAt() — 즉시 정지
StageNavMesh.cpp FindPath() — Detour 길찾기
GameServer.cpp SendMoveNtf(), SendMovePosCorrectNtf() — 패킷 송신

- 클라
MouseInputHandler.cs — 클릭 감지, 200ms 경로 재계산, 500ms MoveDestReq 송신
PlayerCharacter.cs — waypoint 따라 이동, SetPosition 으로 스냅
StageManager.cs onMoveNtf — 다른 캐릭터 위치 갱신 (현재 텔레포트)
StageManager.cs onMovePosCorrectNtf — LocalPlayer 위치 보정


# NavMesh

## 빌드 타임 (Unity 에디터)
- 씬 내의 오브젝트 중 walkable 메시 배치 + NavMeshSource 컴포넌트 부착되어있는 오브젝트가 NavMesh 대상임
- 에디터에서 NavMeshBuildMenu 메뉴로 NavMesh 파일 생성
- NavMeshBuilder.cs 파일에 NavMesh 파일 생성 로직이 있음
- 출력물: 
	- 스테이지이름.bin 파일 (NavMesh 파일)
	- 스테이지이름.bin.navmeta.json 파일 (NavMesh 메타파일. 월드크기 등의 정보가 있음)
- 출력경로:
   - 클라이언트 : Client/Assets/StreamingAssets/NavMesh/
   - 서버 : Server/OUTPUT/Map/NavMesh/

## Stage 씬과 NavMesh 
Unity 에디터에서는 Stage 1개당 별도의 씬을 만든다. (실제 게임 플레이는 Game 씬 1개에서 모두 처리)  
그리고 이 씬은 빌드에 포함되지 않는다.   
이 씬에는 NavMesh용 오브젝트(NavMeshSource 컴포넌트 부착)와 화면표시용 오브젝트가 같이 있다.   
에디터에서 NavMesu 일괄빌드를 하면 Assets/Scenes/NavMeshBuild 폴더내의 씬에서 NavMesh .bin을 생성한다.  

Stage 씬 작업 기본 패턴: Navigation Geometry 분리. Visual mesh (보이는 거) 와 Navigation mesh source (NavMesh 빌드용) 를 분리해서 만든다.
Stage 씬 구조 예시:
├─ Visual/                          ← MeshRenderer 있음. NavMeshSource 없음.
│  ├─ Terrain_Detailed              (고폴리 지형)
│  ├─ Trees, Bushes, Grass          (장식. 작은 풀 등)
│  ├─ Rocks, Props                  (소품)
│  ├─ Buildings_Exterior            (건물 외관, 디테일)
│  └─ Particle Effects, VFX
│
└─ Navigation/                      ← MeshRenderer 끄거나 에디터 전용. NavMeshSource 부착.
   ├─ Walkable_Floor_Plane          (단순한 바닥 메시. 캐릭터가 걸을 수 있는 영역)
   ├─ Walkable_Stairs_Ramp          (계단 대신 ramp)
   ├─ Blocker_Invisible_Wall_01     (보이지 않는 벽. 길막기용)
   ├─ Blocker_Invisible_Wall_02
   ├─ Blocker_Cliff_Edge            (절벽 떨어짐 방지)
   └─ Building_Collision_Simple     (건물의 단순화된 콜리전 = navmesh source)

   
## 게임서버
- 게임서버 가동될 때 NavMeshManager::LoadAll(dirPath) 에서 모든 NavMesh 파일, NavMesh 메타파일 로드함
- 스테이지 생성될 때 NavMeshManager 에서 NavMesh 데이터, 월드크기 등을 가져와서 사용

## 클라이언트
- 게임플레이는 Game 씬에서 모두 처리한다.
- 클라에서 Stage 이동 시 씬 전환 없이 아래 작업들을 수행한다:
	- 1. 현재 Stage 환경 정리 (지형 prefab 제거)
	- 2. 다음 Stage의 NavMesh 데이터를 NavMeshManager 에서 가져와서 활성화
	- 3. (필요시) 다음 Stage용 환경 prefab instantiate
	- 4. LocalPlayer 위치를 새 spawn point로 옮김
	
	
# 버프(Buff) 시스템

## 원칙
- **서버 권위**: 적용/해제/만료/스택/주기효과(DoT·HoT)는 전적으로 서버가 처리한다.
- **클라는 표시만**: 클라는 버프를 계산하지 않고, 서버가 내려준 정보(키/스택/남은시간/카테고리)를 UI로 보여줄 뿐이다. 
- **현재 버프 구현범위**: 스탯 수정 버프 + DoT/HoT(주기 HP 변동). 시전자별 독립 스택은 미지원(액터당 buffKey 1인스턴스).

## 패킷 (서버 → 클라 전용)
버프 패킷은 **UI 뱃지 정보만** 나른다. 실제 효과는 기존 패킷을 재사용한다:
- **스탯 변화** → 기존 `StatUpdateNtf`(합성 스탯 스냅샷)
- **HP 변화(DoT/HoT)** → 기존 `HpMpNtf`

| 패킷 | ID | 내용 | 용도 |
| --- | --- | --- | --- |
| `BuffNtf` | `GAME_PACKET_ID_BUFF_NTF`(7001) | object_id, buff_key, stack_count, remain_time_ms | 버프 추가/갱신(upsert). 같은 (object_id, buff_key) 면 갱신 |
| `BuffRemoveNtf` | `GAME_PACKET_ID_BUFF_REMOVE_NTF`(7002) | object_id, buff_key | 만료/디스펠/정리 공통 제거 |
| `BuffSnapshotInfo` | ObjectVisibilityNtf 에 내장됨 | buff_key, stack_count, remain_time_ms | 시야 진입 시 `CharacterSpawnInfo`/`MonsterSpawnInfo`의 `repeated buffs` 로 현재 버프 동봉 |

- `remain_time_ms = -1` 이면 영구(클라는 카운트다운 표시 안 함).

## 게임데이터 (`GameData_Buff`)
- enum 3종: `EBuffCategory`(Buff/Debuff), `EBuffStackPolicy`(Refresh/Stack/Ignore), `EPeriodicEffect`(DamageHp/HealHp)
- 주요 컬럼: `DurationMs`(-1=영구), `MaxStack`, `StackPolicy`, `TickIntervalMs`(0=주기효과 없음), `PeriodicType`/`PeriodicValue`(flat), `Stat1~3`/`StatValue1~3`(스택당 적용값)
- **스탯은 구성요소 스탯(`*Add`/`*AddPct`/`*Amp`/`*Reduce`)에만 넣는다.** `*Total`은 서버가 자동 재계산하므로 직접 넣지 않는다.
- `Category`/`Name`/`Desc`/`DurationMs`/`MaxStack`/`IconId` 는 클라에도 출력(UI용). 그 외(StackPolicy, Stat 등)는 서버 전용.

## 서버 구조
- `ActorObject` 가 `BuffComponent` 를 멤버로 소유 (Character/Monster 공통).
- `BuffComponent` API: `ApplyBuff(buffKey, casterObjectId)`, `RemoveBuff(buffKey)`, `Update(deltaMs)`, `OnOwnerDead()`, `OnOwnerStageChange()`.
- 스탯 적용은 `ActorObject::GetStatComponent()->ApplyStat/RemoveStat` (Amp/Reduce 비선형 때문에 스택마다 1회씩).
- 변화 통지 훅: `OnStatsChangedByBuff()`(Character→`StatUpdateNtf`), `OnHpChangedByBuff()`(Character→`HpMpNtf`). 몬스터는 no-op.
- 뱃지 송신: `Stage::BroadcastBuffNtf/BroadcastBuffRemoveNtf` 가 AOI(주변+본인) 유저에게 `GameServer::SendBuffNtf/SendBuffRemoveNtf` 로 전송.
- 시간 처리: 절대시각이 아니라 **남은시간/틱누적** 모델. `Update(deltaMs)` 의 deltaMs 가 틱 간격보다 커도(오브젝트 주기가 길어도) 그 사이 놓친 주기효과 틱을 보정해 발사.

## 클라 구조
- `ActorObject`/`MonsterObject` 가 `BuffHolder Buffs` 보유 (서버가 준 키/스택/남은시간 보관).
- `StageManager` 가 `BuffNtf`/`BuffRemoveNtf` 핸들러 등록 + spawn 스냅샷의 buffs 반영.
- UI: `UI_BuffBar`(Game 어셈블리, `UI_Scene`)가 `LocalPlayer.Buffs` 를 읽어 `UI_BuffIcon`(UI 어셈블리, SubItem) 을 생성/갱신/제거.

## 전체 흐름
```
[서버] ApplyBuff / 스택 / 만료 / 주기틱
   ├─ 스탯 변화      → StatUpdateNtf ─→ [클라] StatHolder
   ├─ HP 변화(DoT/HoT) → HpMpNtf      ─→ [클라] CurHp
   └─ 뱃지(키/스택/시간) → BuffNtf/BuffRemoveNtf (+spawn 스냅샷) ─→ [클라] BuffHolder → 버프 바
```

## 현재버전 기본 정책
- 시전자가 죽거나 떠나도 이미 걸린 버프는 유지.
- 스택 정책: Refresh(시간만 갱신) / Stack(스택+1, MaxStack까지) / Ignore(이미 있으면 무시).
- 진입점: 스킬·전투 등에서 `actor.GetBuffComponent().ApplyBuff(buffKey, casterObjectId)` 호출.
